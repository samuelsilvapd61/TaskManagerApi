server:
  port: 8888

spring:
  security:
    basic:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/task_manager_database
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect